# Project Context — Market Intelligence Agent

This file is the memory and rulebook for Claude Code. Read it at the start of every session.

---

## What This Project Is

**Market Intelligence Agent** — an AI-powered system that researches companies, markets, and competitors on demand and returns structured, actionable intelligence reports.

This is not a generic chat app. It is a purposeful AI system with a defined job: take a research query, gather and synthesize information using AI tools and structured reasoning, and produce output that is actually useful to a marketer or strategist.

**Why this project exists:**
The builder is preparing for a technical + creative marketing internship at Hulo. This project is the learning vehicle — each feature teaches a real AI engineering concept that will be valuable on the job and in every future project.

---

## Who This Is For

**Builder:** A marketing student learning to build with AI systems — not a software engineer. Code must be readable, educational, and explained clearly.

**End user of the product:** Someone who needs fast, structured market research — competitive landscape, brand positioning, trend signals — without spending hours doing it manually.

---

## Learning Roadmap

Each phase of this project is designed to teach a specific AI engineering concept:

| Phase | Concept | What gets built |
|-------|---------|-----------------|
| 1 | Claude Code workflow + Git habits | Project setup, version control, documentation |
| 2 | Claude API + API routes | Basic AI query → structured response |
| 3 | Tool calling | Agent that can use search, scrape, or other tools |
| 4 | Structured outputs | Reports with consistent, parseable JSON schemas |
| 5 | Context engineering | System prompts that produce reliable, high-quality intelligence |
| 6 | Agentic workflows | Multi-step research pipelines with intermediate reasoning |
| 7 | RAG + vector databases | Persistent knowledge base the agent can query |
| 8 | Memory systems | Agent that remembers past research sessions |
| 9 | Evaluation systems | Automated quality scoring for agent outputs |
| 10 | Human-out-of-the-loop | Scheduled, automated intelligence delivery |

**Current phase:** Phase 3 → Phase 9 transition. **V3.13 is current.** All five workflow stages (market-signals → candidate-discovery → evidence-validation → opportunity-scoring → opportunity-brief) have live execution paths, gated on the appropriate approvals, and have each run successfully against real Italian water-utility data at both regional (Lombardia/Lazio, Sicilia, Calabria, Trentino, Veneto, Toscana) and national scale. Both Review Gates now have a real CLI (`scripts/review-gate.mjs`, `npm run workflow:review2`) — no more hand-edited JSON for gate 1/gate 2 decisions. `contacts-master.md` consolidates contacts from every Gate-2-approved run: **26/30 toward the contact target**, from 7 approved runs (regional `...736be5a4`, national `...cfa09872`, Sicilia `...b5971aea`, Calabria `...705731b4`, Trentino `...a7c6658d`, Veneto `...6f4bdfee`, Toscana `...cac519a3`). `ASSUMPTIONS-TO-REVISIT.md` now tracks every assignment-specific decision made instead of a genuine production one, for revisiting once real HULO input exists. 221 tests passing (12 eval cases). All work through this point is pushed to `origin/main`. Full session-by-session detail is in Project History below (2026-06-13 through 2026-07-27 entries).

**Known gaps / next candidates:**
- No CSV/spreadsheet export for a hand-off deliverable — current output is JSON + markdown under `runs/<run-id>/` plus `contacts-master.md`, not something to drop into a sales tool yet.
- No EU tenders (TED) integration or curated Italian water-news source list — general web search only.
- LinkedIn: intentionally search-snippet-only, no API/login access — a deliberate scope decision, not a gap, but it caps contact yield.
- Contact target: 26/30, 4 short (after a contact-yield diagnostic — see 2026-07-26 entry — found Calabria's initial 0-contact result was search non-determinism, not a real ceiling; a Stage 05 re-run found 3; Veneto added 2 more; Toscana added 4 more). Trentino's 1-contact result does hold up as a genuine candidate-type effect (17 of 18 candidates are small municipal offices). Growing further means more regions and/or improving Stage 05's hit rate; a low-yield result should be spot-checked or re-run before being read as a structural limit.
- Five hardcoded Italy references live outside the case files (`stage02.mjs`, `stage04.mjs`, both in `02_candidate-discovery/CONTEXT.md` and `05_opportunity-brief/CONTEXT.md`, and `scoring-rubric.md`'s `regionFit` text) — logged in `ASSUMPTIONS-TO-REVISIT.md` item 5. Verified these do not currently narrow Stage 01/02 search within Italy (Stage 01 has none; the one Stage 02 example generalized correctly to a differently-named governance body in real output), but they block running the pipeline for a second country until genericized.

---

## Product Vision

A marketing professional opens the app, types a research question — "Who are Hulo's top three competitors and what is their brand positioning?" — and within seconds receives a structured intelligence report with source reasoning, key findings, and strategic takeaways.

The agent does not just chat. It reasons, gathers, and synthesizes. The output is designed to be dropped into a deck or brief with minimal editing.

---

## V1 Scope

V1 is the smallest thing that demonstrates the core loop:

1. User enters a research query
2. The AI processes it with a structured intelligence prompt
3. The response is returned as a formatted report (not a chat bubble)
4. The report includes: Summary, Key Findings, Strategic Implications

**V1 does NOT include:**
- Real-time web search or scraping (Phase 3+)
- Persistent memory or saved reports (Phase 7+)
- User accounts or authentication
- Database or file storage
- Automated scheduling
- Multi-step agentic pipelines
- Vector search or RAG
- Evaluation scoring

V1 proves the interaction model and report format before adding complexity.

---

## Architecture Principles

1. **All AI calls go through `lib/ai.js`** — no exceptions. Provider-switching must always be a one-line change.
2. **One file, one responsibility** — if a file needs "and" to describe what it does, split it.
3. **No premature abstraction** — build for what is needed now, not hypothetical future features.
4. **Server/client boundary is strict** — secrets and AI calls stay server-side; UI stays client-side.
5. **Structured over conversational** — outputs should be formatted reports, not chat text.

---

## Tech Stack

| Layer | Technology | Why |
|-------|-----------|-----|
| Framework | Next.js 14 (App Router) | Vercel-native, frontend + backend in one project |
| Styling | Tailwind CSS | Fast, consistent, no separate CSS files |
| AI Layer | `lib/ai.js` wrapper | One place for all AI logic |
| Default AI | Anthropic Claude (`claude-sonnet-4-6`) | Best reasoning and structured output quality |
| Deployment | Vercel | Zero-config, integrates with GitHub |
| Language | JavaScript (no TypeScript) | Simpler for a learner; type safety can be added later |

---

## Rules Claude Code Must Follow

### Before writing any code
- Read this file first
- Check recent git history if context is unclear: `git log --oneline -10`
- Inspect the relevant files before proposing changes
- State the current architecture and how the change fits into it
- Propose an implementation plan with the exact files to change
- Wait for approval before making large or architectural changes

### Code quality
- Keep code simple. No over-engineering. One file does one thing.
- No TypeScript unless explicitly requested.
- No features, abstractions, or dependencies beyond what was asked for.
- No comments that explain *what* — only comments that explain *why* when it is non-obvious.

### Security
- Never hard-code API keys. Always use environment variables.
- Validate inputs at every API route before passing them further.
- Never expose internal error details to the frontend.

### Style
- Tailwind CSS for all styling. No inline `style={}` props.
- `PascalCase` for components, `camelCase` for lib files and API routes.
- `@/` path aliases for all imports.

### AI provider
- All AI calls go through `lib/ai.js`. Never import AI SDKs in components or pages.
- Default provider is Anthropic Claude.

---

## How to Explain Changes

Every time a file is created or modified, Claude Code must explain:

1. **What changed** — which file(s) and exactly what was done
2. **Why it matters** — the architectural or product reason for the decision
3. **What to learn from it** — one transferable concept written for a non-developer

Keep explanations short, plain, and jargon-free (or define the jargon in the same sentence).

---

## Browser Testing Workflow

Use Chrome browser testing after any change that affects what the user sees or interacts with.

**Trigger browser testing when:**
- A component, layout, or style was changed
- A form, button, or interactive element was added or modified
- An API route response changed and the UI depends on it
- The app was deployed and the live URL needs verification

**Skip browser testing for:**
- Pure logic or utility changes with no visual output
- Documentation edits
- Environment variable changes

**Standard sequence:**
1. Confirm dev server is running (`npm run dev`)
2. Load the page — verify no blank screen or console errors
3. Test the changed interaction end-to-end
4. Check browser console — zero red errors is the bar
5. Verify edge cases: empty input, loading state, error state

**Scope rule:** Browser testing is for validation only — not autonomous redesign. Report discovered issues before fixing anything outside the original task scope.

---

## Deployment Process

1. Push code to GitHub
2. Import project in Vercel dashboard
3. Add environment variables manually in Vercel (never uploaded from `.env.local`)
4. Vercel auto-deploys on every push to main branch

**Required variables in Vercel:**
- `AI_PROVIDER`
- `ANTHROPIC_API_KEY`
- `NEXT_PUBLIC_APP_NAME`

---

## What Each Key File Does

| File | Purpose |
|------|---------|
| `app/layout.js` | HTML shell — fonts, metadata, global styles |
| `app/page.js` | Homepage — composes the main UI |
| `app/api/chat/route.js` | AI backend endpoint — validates `{ company, industry, focus }`, calls AI, returns report |
| `app/api/mode/route.js` | Safe mode endpoint — returns `{ mode }` (current APP_MODE value) without exposing the env var to the browser |
| `lib/workflow/validate.mjs` | Tiny dependency-free JSON-Schema validator (supports a documented subset) |
| `lib/workflow/schemas.mjs` | Maps artifact names → schema files; validates/asserts artifacts |
| `lib/workflow/runStore.mjs` | Per-run artifact storage under `runs/<run-id>/`; thin filesystem wrapper, swappable for a DB |
| `lib/workflow/mockPipeline.mjs` | Deterministic, free sample artifacts for all five stages (mock-mode engine) |
| `lib/workflow/runWorkflow.mjs` | Orchestrator — runs stages in order, validates, runs integrity checks, persists; refuses cheap/full (no silent spend) |
| `workflows/market-opportunity/` | ICM-style staged workflow: top + per-stage `CONTEXT.md` contracts, JSON schemas, and stable `references/` (rubrics, standards) |
| `evals/cases/` | 5 realistic eval cases (water sector; no confidential data) defining expected characteristics |
| `evals/results/` | Eval run outputs (git-ignored; only `.gitkeep` is tracked) |
| `evals/harness/run.mjs` | Main eval runner — `runEvaluation(caseId, { mode })` runs both adapters and writes all results |
| `evals/harness/adapters/baseline.mjs` | Baseline adapter — mock-safe; wraps the single-report flow (throws on non-mock) |
| `evals/harness/adapters/staged.mjs` | Staged adapter — mock-safe; calls `runWorkflow` and reads back artifacts (throws on non-mock) |
| `evals/harness/adapters/local-stage01.mjs` | Local Stage 01 eval adapter — calls `runWorkflowLocal` via local CLI; used by `eval:local-stage01` only |
| `evals/harness/adapters/future-contract.mjs` | Interface contract for future multi-agent adapters; placeholder only |
| `evals/harness/normalize/` | Maps both raw outputs to a common evaluation structure; baseline mapping is lossy (prose), staged is lossless |
| `evals/harness/checks/deterministic.mjs` | Automated checks: URL validity, duplicate detection, score range, outreach language, mock labelling |
| `evals/harness/rubric/rubric.md` | Human reviewer scoring guide — 8 dimensions with 1/3/5 anchors in marketing language |
| `evals/harness/rubric/review-cli.mjs` | CLI for entering reviewer scores into `human-review.json` |
| `docs/architecture/current-state-audit.md` | The architecture audit that motivated the staged direction |
| `scripts/run-workflow-demo.mjs` | `npm run workflow:demo` — one free mock run |
| `scripts/run-workflow-local.mjs` | `npm run workflow:local -- --case <id>` — Stage 01 live via local CLI (subscription, no API billing) |
| `scripts/review-run.mjs` | `npm run workflow:review -- --run <id>` — review Stage 01 artifacts, record approve/reject |
| `scripts/review-gate.mjs` | `npm run workflow:review2 -- --run <id> --gate 1\|2` — review Gate 1 (Stage 02/03 artifacts) or Gate 2 (Stage 05 briefs/contacts), record approve/changes-requested; writes `metadata.json → reviewGates.gate<N>` + `0X-review-gate.json` |
| `scripts/run-eval-local-stage01.mjs` | `npm run eval:local-stage01 -- --case <id>` — Stage 01 local CLI evaluation with confirmation gate |
| `lib/workflow/providers/localCli.mjs` | Local CLI provider — spawns `claude --print`, strips paid API keys, uses OAuth; injected `spawnFn` for tests |
| `lib/workflow/stage01.mjs` | Stage 01 business logic — loads CONTEXT.md + evidence standards, builds prompt, parses + validates JSON output |
| `lib/workflow/runWorkflowLocal.mjs` | Async Stage-01-only orchestrator for local-cli mode — separate from `runWorkflow.mjs` (mock, unchanged) |
| `lib/workflow/stage02.mjs` | Stage 02 business logic — builds discovery prompt from Stage 01 signals/sources, parses + validates candidates, cross-checks signalId/sourceId refs, drops duplicates |
| `lib/workflow/runStage02Local.mjs` | Continues an already-approved run into Stage 02; refuses to run unless Stage 01 was explicitly approved |
| `lib/workflow/stage03.mjs` | Stage 03 business logic — judges every Stage 01 signal claim and Stage 02 relevanceEvidence claim against `evidence-standards.md`, returns `{ validated, rejected }` with nothing dropped silently |
| `lib/workflow/runStage03Local.mjs` | Continues an already-approved run into Stage 03; refuses to run unless Stage 01 was approved and Stage 02 has completed; this stage is where Review Gate 1 lives |
| `lib/workflow/stage04.mjs` | Stage 04 business logic — scores every Stage 02 candidate on the five ICP dimensions in `scoring-rubric.md`, using only Stage 03's validated evidence; recomputes `totalScore` rather than trusting the model's sum |
| `lib/workflow/runStage04Local.mjs` | Continues an already-approved run into Stage 04; refuses to run unless Stage 01 was approved, Stage 03 has completed, and `reviewGates.gate1` is `"approved"` — the first stage to enforce a human review gate in code |
| `lib/workflow/stage05.mjs` | Stage 05 business logic — writes decision-ready briefs from Stage 02/03/04 artifacts; drops any brief whose `recommendedNextAction` contains outreach language or whose contact has no real `sourceUrl`; renders `05-brief.md` for human reading; `briefAll` option (via `--brief-all`) briefs every candidate surviving Stage 03 instead of the model's curated top-pick subset |
| `lib/workflow/runStage05Local.mjs` | Continues an already-approved run into Stage 05; refuses to run unless Stage 01 was approved and Stage 04 has completed; calls the provider with `tools: ['WebSearch']` only — no WebFetch — so contact-finding cannot fetch any URL, including a LinkedIn profile page; this stage is Review Gate 2's source material |
| `lib/buildContactsMaster.mjs` | Consolidates contacts from every Gate-2-approved run into one markdown table; dedupes by name+company, merges into an existing table without overwriting a manually-set status |
| `scripts/build-contacts-master.mjs` | `npm run contacts:build` — thin CLI wrapper around `buildContactsMaster.mjs`, writes `contacts-master.md` at the project root |
| `contacts-master.md` | Generated (but hand-editable) rollup of every real contact found across Gate-2-approved runs; regenerate after each new gate2 approval — a human's "verified" status edits survive regeneration |
| `tests/` | `node:test` suites for validator, schemas, run store, and the mock pipeline |
| `runs/` | Local run artifacts (git-ignored except README) |
| `lib/ai.js` | AI wrapper (Anthropic-only) — `chat()` for single calls, `chatWithTools()` for the generic agentic loop; only place where AI SDKs are imported; exports `MODELS` (full = Sonnet, cheap = Haiku) |
| `lib/appMode.js` | Cost-control mode switch — resolves `APP_MODE` env var (`mock` / `cheap` / `full`) and per-mode settings (model, max tokens, max searches); defaults to mock in dev, full in production |
| `lib/mockReport.js` | Static sample report for mock mode — same structure as real output so the UI renders it identically; zero API cost |
| `lib/focusOptions.js` | Shared research-focus constants — imported by both form and API so they never drift |
| `lib/tools/webSearch.js` | Tavily web search — tool schema Claude sees + the fetch that executes it; one file per tool |
| `lib/prompts/marketIntelligencePrompt.js` | The analyst system prompt — report structure, data-honesty rules, depth weighting |
| `components/ResearchForm.js` | Research form — owns query/report/loading/error state |
| `components/ReportView.js` | Parses the markdown report into styled sections; copy-to-clipboard |
| `components/ChatBox.js` | Legacy chat UI — kept as reference, no longer rendered |
| `.env.local` | Real secrets — never committed |
| `project_context.md` | This file — Claude Code reads it every session |
| `ASSUMPTIONS-TO-REVISIT.md` | Tracks decisions made for the internship assignment rather than genuine production/HULO judgment (scoring dimensions, `CONTACT_TARGET=30`, `--brief-all` as coverage mode, region-specific cases, Italy scope, target role categories, NRW-narrow signal tuning, missing outreach tracking) — each with what it should become once real HULO input exists; log a new entry whenever an assignment-specific choice gets made instead of a general one |

---

## Future Phases

### Phase: Visual dashboard for pipeline output
**Status:** Not started — deliberately deferred.

**Trigger to start:** only once the pipeline has been generalized to a second vertical/ICP beyond Italian water utilities (see `ASSUMPTIONS-TO-REVISIT.md`), and only when the goal shifts to demoing/pitching the system to external prospects.

**Scope when started:** a simple, functional dashboard — NOT a decorative graph visualization. Needed views:
- Table of leads ranked by score, with evidence/justification visible per score
- Simple funnel view: signals found → candidates → validated → scored → briefed → contacted
- One summary screen showing current run status/output

**Explicitly out of scope:** force-directed node graphs, animated visualizations, or any UI complexity that doesn't directly help a prospect evaluate the tool in under 2 minutes.

---

## Project History

- **2026-05-09** — Created as a generic AI starter template
- **2026-06-04** — Renamed and repurposed as Market Intelligence Agent; documentation updated to define product vision, learning roadmap, V1 scope, and architecture principles
- **2026-06-04** — V1 implemented: research form (company, industry, focus) → structured seven-section report via `lib/prompts/marketIntelligencePrompt.js`; AI wrapper made Anthropic-only
- **2026-06-11** — V1.1 polish: report rendered as styled sections via `components/ReportView.js` (hand-rolled parser, zero new packages); system prompt upgraded with data-honesty rules, focus-weighted depth, and a closing confidence note; shared `lib/focusOptions.js` validates focus on both client and server; clearer loading/error states
- **2026-06-11** — V2.0 live search plumbing: `lib/tools/webSearch.js` calls Tavily via plain fetch; route runs 3 fixed parallel searches (overview, competitors, recent positioning) and injects numbered, trimmed sources into the prompt; reports cite sources inline and end with a Sources Used section; graceful fallback to model-knowledge-only when `TAVILY_API_KEY` is missing or searches fail; UI shows grounding status. No tool-use loop yet — that is V2.1
- **2026-06-11** — V2.1 agentic tool calling: `chatWithTools()` added to `lib/ai.js` — a generic tool-use loop (model requests tool → route executes → result returned → repeat until plain-text answer, hard-capped at 4 tool calls + runaway round limit). Claude now composes its own search queries instead of the fixed three; failed searches return `is_error` tool results and the model continues gracefully. Route tracks sources via closure and returns `{ reply, sourceCount, toolCallCount, sources }`; UI badge shows search count. System prompt caching enabled across loop rounds
- **2026-06-12** — Billing errors surfaced clearly: the API route maps Anthropic "credit balance too low" failures to an actionable error message instead of the generic fallback
- **2026-06-12** — Cost-control modes: one `APP_MODE` env var with three values — `mock` (static sample report from `lib/mockReport.js`, zero API calls, default in local dev), `cheap` (Haiku model, 1 search, 1.5K max tokens, brief report), `full` (unchanged complete agentic workflow, default in production). Mode resolution lives in `lib/appMode.js`; the route branches once at the top; `chat()`/`chatWithTools()` accept `model`/`maxTokens` options. UI shows an amber badge on mock/cheap reports; server logs the active mode per request. Documented in README and `.env.example`
- **2026-06-13** — Safety hardening on APP_MODE: production default changed from `full` → `cheap` with a `console.warn`; full mode now requires `APP_MODE=full` set explicitly everywhere. Added `GET /api/mode` route (`app/api/mode/route.js`) so the UI can safely display the active mode without exposing `APP_MODE` as a `NEXT_PUBLIC_` variable. `ResearchForm.js` now fetches active mode on mount and shows a pre-generation indicator; post-report badges corrected (cheap badge says "reduced-cost real AI report", full mode gets its own green badge).
- **2026-06-13** — V3.0 foundation: product direction expanded to a B2B Market Opportunity & Account Intelligence System, built baseline-first. Added the staged ICM-inspired workflow `workflows/market-opportunity/` (5 stages: market-signals → candidate-discovery → evidence-validation → opportunity-scoring → opportunity-brief), each with a `CONTEXT.md` input/process/output contract, JSON schemas, and shared stable `references/` (scoring rubric, evidence standards). New `lib/workflow/` engine: a dependency-free JSON-Schema validator, a schema loader/asserter, per-run artifact storage (`runs/<run-id>/`), a deterministic free mock pipeline, and an orchestrator that validates every artifact + runs cross-stage integrity checks and **refuses cheap/full to avoid silent API spend**. Two human review gates defined (after validation; before briefs are final). Evaluation foundation in `evals/` (12 dimensions + 5 realistic water-sector test cases, no confidential data). Tests via Node's built-in `node:test` (zero new dependencies) — 23 passing. The existing baseline report flow, UI, modes, and citations are untouched. **What's postponed:** live cheap/full stage execution, any multi-agent orchestration, a review UI, and a DB — all gated on the eval harness showing measurable improvement.
- **2026-06-14** — V3.1 eval harness: full evaluation infrastructure built. `evals/harness/` contains baseline and staged adapters (mock-safe, throw on live modes), output normalizers (lossless for structured artifacts, heuristic for prose), deterministic checks (URL validity, duplicate detection, score range, outreach language, mock labelling), a human review rubric with 1/3/5 anchors in marketing language, and a review CLI. `evals/results/` is git-ignored. `npm run eval:mock` runs all 5 cases through both adapters in ~0ms at zero cost. `npm run eval:live` is ready infrastructure with cost estimates and explicit confirmation — live stage execution wiring is the next step. Tests: 55 passing (32 new eval harness tests, 23 existing). Baseline flow unchanged.
- **2026-06-15** — V3.2 local-cli provider: clean provider abstraction for Stage 01 research using the locally-authenticated Claude Code CLI (OAuth session, no paid API billing). New files: `lib/workflow/providers/localCli.mjs` (subprocess spawner with injected spawnFn for testing, safe environment — strips ANTHROPIC_API_KEY, disabled in production), `lib/workflow/stage01.mjs` (prompt builder from CONTEXT.md + evidence standards, JSON parser, schema validator per item), `lib/workflow/runWorkflowLocal.mjs` (async Stage-01-only orchestrator, writes review-gate.json, preserves raw output). Developer scripts: `run-workflow-local.mjs`, `review-run.mjs`, `run-eval-local-stage01.mjs`. Schema updates: `run-request` and `run-metadata` schemas now accept `local-cli` as a valid mode. Tests: 87 passing (32 new, zero real CLI calls in test suite). Stages 02–05 and existing mock/baseline flows untouched.
- **2026-07-22/23** — V3.3 Stage 02 live + Italy data: `lib/workflow/stage02.mjs` + `runStage02Local.mjs` add a live candidate-discovery path, gated on Stage 01's review-gate approval — same pattern as Stage 01. `organisationType` enum extended with `multi-utility-company`. Both Stage 01 and Stage 02 then ran successfully against real Italy data (16 signals/14 sources → 10 candidates, 0 rejected), reviewed and approved. Fixed a real evidence-quality bug found on that run: regional-vs-national figures were wrongly flagged as "conflicting" when they were just different scopes — added a scope-before-conflict rule to `evidence-standards.md`. 119 tests passing.
- **2026-07-23** — V3.4 Stage 03 live: `lib/workflow/stage03.mjs` + `lib/workflow/runStage03Local.mjs` (`npm run workflow:stage03 -- --run <id>`) add a live evidence-validation path, same pattern as Stages 01–02 — refuses to run unless Stage 01 is approved and Stage 02 has completed. Judges every signal claim and candidate relevanceEvidence claim against `evidence-standards.md`'s keep/reject rules, producing `03-validation.json` (`{ validated, rejected }`); a claim can never silently disappear — it is dropped-with-warning only for schema/reference errors, otherwise it lands in exactly one of the two lists. This stage is where Review Gate 1 lives per the CONTEXT.md contract; `reviewGates.gate1` stays "pending" in `metadata.json` until a person reviews `02-candidates.json` + `03-validation.json` together (no dedicated review CLI for gate1 yet — read the JSON directly, same as Stage 02 today). 142 tests passing (23 new). Not yet run against real Italy data.
- **2026-07-23** — Stage 03 run live against Italy (`runs/...864fc892`): 8 validated, 26 rejected, 0 dropped by schema/reference checks, 309s (needed `LOCAL_CLAUDE_TIMEOUT_MS` raised to 360s — bumped from the 180s default afterward). Rejections matched evidence-standards correctly (recency, weak Tier-C sourcing, unfetchable sources, duplicates); the scope-vs-conflict rule worked end-to-end, correctly rejecting a claim that tried to frame regional-vs-national NRW figures as conflicting. Review Gate 1 approved by hand.
- **2026-07-23** — V3.5 Stage 04 live: `lib/workflow/stage04.mjs` + `lib/workflow/runStage04Local.mjs` (`npm run workflow:stage04 -- --run <id>`) — opportunity scoring, same live pattern as Stages 01–03, but with a stricter gate: refuses unless Stage 01 approved, Stage 03 completed, **and** `reviewGates.gate1` is literally `"approved"` — the first stage to enforce a human review gate in code, since scores are the first artifact a human might act on directly. Scores every candidate on the five ICP dimensions in `scoring-rubric.md` using only Stage 03's *validated* evidence; `totalScore` is recomputed server-side, never trusted from the model. 162 tests passing (20 new). Run live against Italy: all 10 candidates scored, 0 dropped, 171.6s — Uniacque (20/25), MM S.p.A. (18/25), EGATO5 (17/25) led. Review surfaced two real problems: 4 of 10 candidates weren't buyers at all (vendors Aganova/WaterTech, regulator ARERA, regional government Regione Lombardia), and `utilitySize` scored weak-to-absent everywhere because no size/scale figures survived Stage 03 validation.
- **2026-07-23** — Fixed both Stage 04 findings, same session: Stage 02's `CONTEXT.md` + `stage02.mjs` prompt gained an explicit `organisationType` scope rule tied to the run's `targetCustomerType` (regulators/competitors only valid when the run itself asks for that landscape — preserves the legitimate `evals/cases/04-leak-detection-competitors-europe.json` use case, so the schema enum wasn't narrowed), and `public-water-organisation` now requires direct water-infrastructure ownership/procurement authority, not just general government. Stage 01 gained a new `organisation-scale` signalType and an explicit instruction to search for population/network-scale figures. Enforcement is prompt-level + Gate 1 review, matching how the rest of evidence-standards works. 162 tests still passing. Verified with a fresh live Italy run (`runs/...736be5a4`, case `06-water-utilities-italy`): Stage 01 produced 5 organisation-scale signals; Stage 02 produced only 6 candidates, all genuine buyers — zero vendors/regulators/government bodies; Stage 04's `utilitySize` scores rose to 2-5 across the board. `DEFAULT_TIMEOUT_MS` in `providers/localCli.mjs` permanently raised 360s→540s afterward (the extra search query needed it).
- **2026-07-23/24** — V3.6 Stage 05 live: `lib/workflow/stage05.mjs` + `lib/workflow/runStage05Local.mjs` (`npm run workflow:stage05 -- --run <id>`) — opportunity briefs, gated on Stage 01 approval + Stage 04 completion. Writes `05-briefs.json` + human-readable `05-brief.md` (Review Gate 2's source material). Drops any brief with outreach language in `recommendedNextAction` or a contact with no real `sourceUrl`. `providers/localCli.mjs` gained an optional `tools` parameter; Stage 05 is the first caller to pass `tools: ['WebSearch']` only, so the CLI is structurally unable to fetch any URL (including a LinkedIn profile page) during contact-finding, rather than relying on the prompt alone. 190 tests passing (28 new). Run live against the regional Italy run: 4 of 6 candidates briefed, 1 real contact found safely via `linkedin-search`, the model caught and excluded its own risky contact misattribution. Review Gate 2 approved, with one correction — a brief cited a €73M total investment figure for BrianzAcque; human verification found the correct total was €60M (€50M PNRR-funded). Corrected across the run's working artifacts; raw provider logs left untouched as the honest record of what the model actually said.
- **2026-07-24** — Wider region coverage run (`runs/...cfa09872`, case `07-water-utilities-italy-national` — same ICP, region restriction dropped): full Stage 01→05 live at national scale. Stage 01: 49 signals/57 sources across 9+ regions. Stage 02: 14 candidates, zero vendors/regulators/government bodies — the Stage 02 tightening held at ~2.3x the candidate volume. Stage 03: 42 validated/43 rejected (one transient CLI connection error, clean on retry). Stage 04: all 14 scored, 0 dropped, range 14-20. Stage 05 (curated mode): 8 of 14 briefed, 1 correctly dropped for outreach language, 2 contacts found. Top candidate: SMAT (Torino), 20/25. Added `evals/cases/07-water-utilities-italy-national.json`, which broke 2 tests hardcoding the eval-case count (6→7) — fixed. 190 tests passing. Review Gate 2 approved, no corrections needed.
- **2026-07-24** — V3.7 contacts-master.md: `lib/buildContactsMaster.mjs` + `scripts/build-contacts-master.mjs` (`npm run contacts:build`, `CONTACT_TARGET=<n>`) consolidate contacts from every Gate-2-approved run into one markdown table (name, role, company, region, source, source type, status). Duplicates skipped by name+company; a manually-set `verified` status survives re-runs. Testable against a temp dir, same pattern as every workflow stage. 204 tests passing (14 new). Initial state: 3/30 contacts from 2 approved runs.
- **2026-07-24** — V3.8 Stage 05 `--brief-all` flag: `npm run workflow:stage05 -- --run <id> --brief-all` briefs every candidate that survives Stage 03 validation (has a `relevanceEvidence` sourceId also cited by a validated evidence item — a mechanical link, not text matching) instead of the model's curated top-pick subset; default behavior unchanged. 215 tests passing (11 new). Run live against the national run: all 14 briefed (vs. 8 curated), 11 contacts found (vs. 2 curated — live search is non-deterministic). This overwrote the already-Gate-2-approved curated version; backed it up to `gate2-approved-curated-backup/` first. Chose to keep the brief-all version and reset `reviewGates.gate2` to `"pending"` for re-review rather than restore the backup. That exposed a real bug in `buildContactsMaster.mjs`: it only ever added rows and never re-checked whether a contact's source run was still approved, so unreviewed contacts would have stayed in `contacts-master.md` indefinitely. Fixed: membership is recomputed from the current set of gate2-approved runs on every build; only `status` carries over. 217 tests passing (2 new). Reviewed and approved the brief-all version; `contacts-master.md` regenerated to 12/30 — the two contacts from the superseded curated version correctly dropped out.
- **2026-07-24** — V3.9 Review Gate CLI: `scripts/review-gate.mjs` (`npm run workflow:review2 -- --run <id> --gate 1|2 [--decision approve|changes-requested] [--note "..."]`, plus `--list` and an interactive mode) replaces hand-edited JSON for gate 1 (after Stage 03) and gate 2 (after Stage 05) decisions — the last review step still done by hand-editing `metadata.json`/`0X-review-gate.json`, mirroring the pattern `scripts/review-run.mjs` already established for Stage 01. Gate 1 mode displays Stage 02 candidates and Stage 03 validated/rejected evidence; gate 2 mode displays Stage 05 briefs, contacts, and `humanVerificationRequired` items. Refuses to run if the prerequisite stage (evidence-validation / opportunity-brief) hasn't completed. Writes the decision to `metadata.json → reviewGates.gate<N>` — validated against the existing `run-metadata` schema via `assertArtifact` before writing, so a bad status value can't be recorded — and to a gate-record file (`03-review-gate.json` / `05-review-gate.json`, the same file gate 2 already used when hand-edited). No new tests: matches the existing convention that CLI wrapper scripts under `scripts/` aren't unit-tested, only the `lib/workflow/*.mjs` modules they call; verified manually against a real run copy (approve path, decision + metadata written correctly) and against a run with no completed stages (precondition correctly refuses). 217 tests still passing (no test-affecting code changed). Tracked in `todo.md` item 7, now closed.
- **2026-07-24** — V3.10 ASSUMPTIONS-TO-REVISIT.md: new root-level file tracking every decision made for the internship assignment rather than genuine production/HULO judgment — the 5 scoring dimensions, `CONTACT_TARGET=30`, `--brief-all` as the default coverage mode, region-specific eval cases, Italy as the fixed country scope, the four target role categories (sourced from the application form, not independent buyer research), the pipeline's NRW-narrow signal tuning (may be narrower than HULO's actual offering), and `contacts-master.md`'s missing outreach-status tracking. Each entry states what it should become once real HULO input exists. New rule going forward: flag assignment-specific choices in the moment so they get logged here rather than left implicit. Verified item 5's original portability claim by grepping `lib/`, `scripts/`, `app/`, `components/`, `workflows/` (including schemas) for `italy`/`italian`: the claim that "only the case files are Italy-specific" didn't hold — found 5 hardcoded Italy references in live prompt-building code and stage contracts (`stage02.mjs`, `stage04.mjs`, both Stage 02 and Stage 05 `CONTEXT.md` files, and `scoring-rubric.md`'s `regionFit` definition); schemas themselves are clean. Corrected item 5 with the specific file:line findings. Separately checked whether any of the 5 are currently causing search tunnel vision (not just a future-portability issue): only one — the Stage 02 "Italian ATO" example — touches Stage 01/02 at all (Stage 01 has zero hardcoded Italy text), and cross-checking against real Stage 02 output across three runs showed it generalizes correctly to differently-named governance bodies (e.g. `EGATO5`), so no evidence of regional tunnel vision in practice today. All changes pushed to `origin/main`.
- **2026-07-26** — V3.11 Calabria regional run, full Stage 01→05 live, both gates approved (`runs/2026-07-26T10-37-38-791Z__705731b4`, new case `evals/cases/09-water-utilities-italy-calabria.json`). Added a new `## Future Phases` section to this file and a matching `## 9. Visual dashboard (future)` section to `todo.md` — a deferred, no-code roadmap placeholder for a lead-ranking/funnel dashboard, gated on generalizing to a second vertical and a prospect-demo need. Stage 01: 29 signals/11 sources on first pass. Human fact-check corrected sig-29 (originally framed Calabria's Oct-2022 second-PNRR-tranche eligibility as progress; corrected to reflect a distress pattern — a separate €104M React-Eu bid rejected March 2022, and a €32.9M PNRR application "admitted but not financed," scoring last among all regions, evaluated Jan 2023) and added sig-30 researching the SFNIISSI 2026 €1B water fund (application window closed 8 June 2026; found no public confirmation of whether Calabria/Sorical/Arrical applied or the outcome, flagged as a material open question for Stage 04's `momentumSignal`). Both re-sourced with 3 new source records (src-12/13/14), schema-validated before Stage 01 approval. Stage 02: only 1 candidate, Sorical S.p.A. — genuinely low vs. Sicilia/national because Sorical is now the sole regional integrated-water-service operator, having consolidated ~400 formerly-independent municipal systems; ARRICAL correctly excluded as a regulator, not a utility. Stage 03: 26 validated / 9 rejected (6 weak-evidence, all Tier-C-only; 1 outdated funding-progress figure; 2 duplicates). Before Gate 1, human review caught a real cross-reference gap: ev-16 ("€32.8M PNRR funding secured," StrettoWeb May 2024) and ev-22 (the corrected sig-29, documenting a Jan 2023 rejection of the same 21-municipality/~€32.9M funding line) were the same funding thread at two different points in time, with nothing linking them — risking Stage 04 scoring ev-16 as clean momentum with no rejection context. Fixed by adding a `relatedEvidence` field (schema allows extra properties; same pattern used for Stage 03 elsewhere) cross-referencing ev-16 ↔ ev-22, and to ev-25 (Stage 02's bundled version of the same claim, same disconnect found on inspection). Gate 1 approved. Stage 04: Sorical scored 22/25 initially, but momentumSignal's justification leaned on ev-23 (the SFNIISSI signal) as positive momentum despite ev-23's own uncertainty note stating no confirmation exists that Sorical/Arrical ever applied — flagged as exactly the score-inflation risk the SFNIISSI research was meant to guard against. Corrected: momentumSignal recomputed from ev-18/19/20 only (3/5 — credible but single-sourced, src-8, ~17 months old, short of the rubric's ≤12-month bar for a top score), totalScore corrected to 21/25, and a new `openOpportunities` field added (same additive-field pattern) carrying ev-23 forward to Stage 05 as sales context without it counting toward the score. Stage 05 (`--brief-all`): 1 briefed, 0 dropped; verified `openOpportunities` actually surfaced in the brief text (Uncertainties + Human Verification Required) rather than being silently dropped as an unhandled new field — it did, in close to the exact language specified. Zero contacts found (honest empty result, no named individual confirmed). Gate 2 approved. Added `evals/cases/09-water-utilities-italy-calabria.json` (9th eval case at the time), which broke the 2 tests hardcoding the eval-case count (8→9) — fixed.
- **2026-07-26** — Trentino regional run, full Stage 01→05 live, both gates approved by the user directly in a separate session (`runs/2026-07-26T12-50-05-944Z__a7c6658d`, new case `evals/cases/10-water-utilities-italy-trentino.json`) — discovered mid-session when `contacts:build` picked up a 5th approved run unexpectedly; confirmed with the user before treating it as legitimate rather than assuming. 18 candidates (Novareti S.p.A. plus 17 municipalities), 33 validated/8 rejected, Novareti scored highest at 22/25, all 18 briefed with 1 contact found (Matteo Frisinghelli, Novareti). Eval case count now 10; both hardcoded-count tests already reflected this. `contacts:build` run after both Calabria and Trentino: **17/30 contacts, from 5 approved runs** (up from 16/30 after Sicilia). 217 tests passing throughout both runs.
- **2026-07-25** — Sicilia regional run (`runs/...b5971aea`, new case `evals/cases/08-water-utilities-italy-sicilia.json`): chosen over rerunning the national case, since a rerun of the same query tends to resurface candidates already found rather than new ones. Full Stage 01→05 live. Stage 01: 37 signals/33 sources. Stage 02: 7 candidates, all genuine utilities/governance bodies, 0 non-buyer leakage. Stage 03: 26 validated/29 rejected (mostly dedup + appropriate skepticism). Before Stage 04, added a rubric clarification to `scoring-rubric.md`: "distress ≠ momentum" — a candidate's debt/financial strain caused by unresolved water issues is context, not a positive `nrwEvidence`/`momentumSignal` signal, since those dimensions require an active, named response. Verified this held in practice: AICA's brief cited €23M in debt tied to unresolved losses but no named initiative, and Stage 04 scored it 0/0 on those two dimensions (lowest total, 9/25), explicitly citing the new rubric line in its justification. Stage 04 scores: AMAP 19, AMAM 18, Siciliacque 17, ATI Trapani 16, Acoset 11, ATI Ragusa 10, AICA 9. Stage 05 (`--brief-all`): all 7 briefed, 5 new contacts found (Massimo Burruano/Siciliacque, Giovanni Sciortino/AMAP, Davide Maimone/AMAM, Pierluigi Carugno/ATI Trapani, Bartolo Giaquinta/ATI Ragusa); AICA and Acoset honestly returned no contacts. Gate 2 human review caught a real correction: the ATI Trapani brief said "no leak-reduction-specific initiative is named" in its €806M Piano d'Ambito, but human verification (3 independent Italian news sources) found the plan explicitly names loss reduction and digitalization as distinct funded categories. Added as new evidence (`sig-38`/`ev-58`, sources `src-34`/`src-35`) and corrected across `02-candidates.json`, `04-scores.json` (`nrwEvidence` 1→4, total 16→19, now tied for 2nd-highest), `05-briefs.json`, and `05-brief.md` — same "correct the artifacts, leave raw provider logs as the honest record" pattern as the BrianzAcque fix. Adding the 8th eval case broke the 2 tests hardcoding the case count (7→8) — fixed. Running `contacts:build` afterward exposed a second real bug: the same real person (Giovanni Sciortino/AMAP) was double-counted across two runs because Stage 02 named the candidate slightly differently each time ("AMAP S.p.A." vs "AMAP S.p.A. (Azienda Municipalizzata Acquedotto Palermo)"), and the dedupe key was a literal lowercased string match. Fixed in `lib/buildContactsMaster.mjs`: dedupe key now strips any parenthetical qualifier from the company name before comparing. 217 tests passing throughout. `contacts-master.md`: 16/30 contacts (was 12/30), from 3 approved runs.
- **2026-07-26** — Trentino regional run started (`runs/2026-07-26T12-50-05-944Z__a7c6658d`, new case `evals/cases/10-water-utilities-italy-trentino.json`), run in parallel with the in-progress Calabria run under a separate run-id — no shared artifacts. Before Stage 01, verified Trentino has multiple distinct gestori (Novareti S.p.A., Trentino Acque S.r.l., smaller municipal operators) rather than a single consolidated regional monopoly like Calabria's Sorical, so it wasn't at risk of the same 1-candidate Stage 02 ceiling. Stage 01: 20 signals/14 sources. Human fact-check before Gate 1 approval surfaced three corrections, logged as the review note on `review-gate.json` (same "correct before it propagates" pattern as the BrianzAcque/ATI Trapani fixes, but caught pre-Stage-02 here since the fact-check ran before candidate discovery): (1) four different NRW percentages surfaced for overlapping scopes — Novareti's own network (~15%, sig-9/10, direct quote from the utility's service head) vs. three regional/national aggregates (25.6%/30%/37.1%, sig-6/14/15) that don't fully reconcile with each other; decided to use the 15% Novareti figure as the entity-level scoring input and treat the regional numbers as noise at the candidate-ICP level, not to be blended in; (2) Novareti (sig-6/7) and "Dolomiti Reti S.p.A." (sig-8) report similar-but-not-identical scale figures under the same Dolomiti Energia group — resolved, not left open: Novareti is the customer-facing operating company (runs captazione/potabilizzazione/distribuzione, owns the leak-reduction budget/decision) and is the Stage 02 candidate; Dolomiti Reti (formerly Avisio Energia, acquired by Dolomiti Energia in 2009) is the same-group infrastructure/network-holding company with no independent customer-facing operations and is not a separate lead. Added an explicit parent/subsidiary entity-scope rule to `02_candidate-discovery/CONTEXT.md` and mirrored it in `stage02.mjs`'s system prompt and user-message builder (same pattern as the earlier `organisationType` scope rule) so the model doesn't independently emit both as candidates; (3) "Trentino Acque S.r.l." (sig-17, Lavis) turned out on registry lookup to be a small private water-treatment engineering firm, not the province-wide multi-utility its name suggests — flagged to exclude from candidate discovery unless a distinct larger entity of the same name turns up. Funding structure (PNRR Mission 2/4.2, €106M/18 municipal projects), digitalisation signals, and competitor scoping (Onyax, Iren) checked out as reasonably sourced, no changes needed. Gate 1 approved via `npm run workflow:review` with the above note attached. Stage 02 not yet run.
- **2026-07-26** — Stage 02 run for Trentino: 18 candidates (Novareti + 17 individual municipalities named in the PNRR funding list, matching sig-18's fragmented-structure finding). The parent/subsidiary fix held: Novareti appears exactly once; its own `uncertainty` field explicitly reasons through why Dolomiti Reti is not a second candidate. "Trentino Acque S.r.l." correctly excluded. Stage 03: 33 validated/7 rejected (all 7 rejections were Stage 01 signal claims — Dolomiti Reti's scale figures, Novareti's loss-target/smart-meter/2021-pilot claims, Onyax, and Iren — none candidate-blocking; every one of the 18 candidates retained validated evidence). Gate 1 approved. Stage 04: all 18 scored, 0 dropped; Novareti led at 19/25 (utilitySize 5, nrwEvidence 5, regionFit 5, targetRolePresence 3, momentumSignal 1 — the rest all scored 12-15).
- **2026-07-26** — Found a real systemic Stage 03 bug during review of Novareti's score: the same underlying facts (Novareti's loss-reduction target, smart-meter rollout) were rejected as `outdated`/`weak-evidence` when evaluated as individual Stage 01 signals (`ev-35`, `ev-36`), but a bundled candidate-evidence claim restating the identical facts from the identical sources was validated anyway (`ev-16`, `recencyOk:false`, confidence 0.6) — bundling several facts into one claim let weak evidence pass that would have been rejected on its own. Fixed at the policy level, same two-file pattern as the parent/subsidiary and organisationType fixes: added a "Same-fact consistency across restatements" rule to `evidence-standards.md` (a bundled claim inherits the rejection of any individual fact it restates from the same sources; split the bundle rather than validating/rejecting it uniformly) and mirrored it in `stage03.mjs`'s system prompt and user-message builder. 217 tests still passing. This is a prospective fix (applies to future Stage 03 runs); the already-generated `ev-16` in this run's `03-validation.json` was not retroactively re-evaluated, since Stage 03 wasn't rerun.
- **2026-07-26** — Added real evidence for Novareti's `momentumSignal`, same "add real evidence found via human verification" pattern as the Sicilia/ATI Trapani correction: searched for and verified a live, currently-executing PNRR project in Rovereto (one of Novareti's own service municipalities) — a €3.5M water-network project (€2.5M PNRR-funded, ~€1M municipality-funded) dividing the network into 21 monitored districts with pressure-reduction valves, leak-detection software, and ~6,000 SmartMeter replacements, contract formalised end-2024, construction underway during 2025. Two independently dated, real sources fetched and confirmed: the Comune di Rovereto's own communiqué (Tier A, government, dated 2024-12-17) and il Dolomiti trade press (Tier B, dated 2024-09-18) — both recencyOk within the 24-month funding/tender threshold. Added as `src-15`/`src-16`, `sig-21`, a new `relevanceEvidence` item on Novareti in `02-candidates.json`, and `ev-41` (validated) in `03-validation.json`. Re-scored Novareti's `momentumSignal` in `04-scores.json`: 1/5 → 4/5 (held below 5/5 since the exact completion date varies slightly between the two sources and no 2026 progress update was found), `totalScore` 19 → 22, now the clearest momentum signal of any candidate in the run. All edited artifacts re-validated against their schemas (`validateArtifact`) before and after; 217 tests still passing (no test-affecting code changed by this correction). The `ev-16` bundling inconsistency flagged above was left as-is for this specific artifact — a separate, known residual issue, not corrected retroactively without a Stage 03 rerun.
- **2026-07-26** — Cleaned up `ev-16` before Gate 2: it was still actively feeding Novareti's `nrwEvidence` justification in `04-scores.json` (not inert), so it needed fixing, not just flagging. Applied the new same-fact-consistency rule retroactively to this one item: moved `ev-16` from `validated` to `rejected` (reason `duplicate`) in `03-validation.json`, since every true part of it (the 15% figure, the monitoring/cartography/pressure-management/acoustic-detection facts) is already separately validated as `ev-8`/`ev-9`, and every part beyond that (the 10% target, the smart-meter rollout) was already independently rejected as `ev-35`/`ev-36` — the bundle contributed nothing that survives on its own. Updated Novareti's `nrwEvidence` justification in `04-scores.json` to cite `ev-8`/`ev-9` instead; the score itself stayed at 5/5, since `scoring-rubric.md`'s own 5/5 bar (a reported NRW% + a named active leak-reduction programme) is still independently met without the removed claims — `totalScore` unchanged at 22. All edited artifacts re-validated against their schemas; 217 tests still passing.
- **2026-07-26** — Stage 05 (`--brief-all`) run for Trentino: all 18 candidates briefed, 0 dropped. Novareti led with a clean fact/interpretation split (verified facts: scale, group relationship, 15% loss, active monitoring stack, the new Rovereto PNRR project; interpretations clearly separated). 1 contact initially found (Francesco Faccioli) across all 18 candidates; the 17 municipalities returned none — honest, expected for small local offices. Before Gate 2, human verification found the initial Novareti contact was misattributed: Francesco Faccioli's actual primary role is CEO & Operations Manager at **SET Distribuzione**, a different Dolomiti Energia subsidiary (electricity distribution, not water) — his Novareti link appears to be a board/director seat, not day-to-day operational leadership. Same class of correction as the BrianzAcque/ATI Trapani/Sicilia fixes this session: searched specifically for a better-evidenced person instead — Matteo Frisinghelli, already named and quoted in Stage 01's own `src-1` (il Dolomiti, 2023) as "responsabile del servizio idrico" discussing this exact water-loss topic on Novareti's behalf. Confirmed via a fresh LinkedIn search-result title only (no profile page visited/scraped, same no-WebFetch contact-finding rule as every other run) as "Responsabile Operativo Servizio Idrico - NOVARETI S.P.A." — two independent, on-topic corroborating mentions rather than one. Replaced the contact in `05-briefs.json` and `05-brief.md` (Frisinghelli, `operations` category, `linkedin-search` sourceType, confidence 0.75) and updated the human-verification note accordingly. Both artifacts re-validated against schemas; 217 tests still passing.
- **2026-07-26** — Trentino run (`runs/2026-07-26T12-50-05-944Z__a7c6658d`) fully complete: both Review Gates approved, Stage 01→05 all live. Novareti S.p.A. is the standout candidate (22/25), with 1 verified-sourced contact (Matteo Frisinghelli). The 17 municipality candidates carry no contacts and are weak-evidence leads individually, but were kept in per this session's explicit instruction not to pre-filter before seeing validation/scoring results. Not yet run: `npm run contacts:build` to fold Frisinghelli into `contacts-master.md` — deliberately left undone this session per the standing instruction not to touch `contacts-master.md` while the parallel Calabria session is active, to avoid a write conflict. Next step for whoever picks this up: run `contacts:build` once both Trentino and Calabria are done being edited in parallel.
- **2026-07-26** — Veneto regional run, full Stage 01→05 live, both gates approved automatically per the user's standing instruction to run all five stages back-to-back and only stop for genuine ambiguity/systemic issues/wrong candidates/judgment calls — none of those came up this run (`runs/2026-07-26T15-29-15-127Z__6f4bdfee`, new case `evals/cases/11-water-utilities-italy-veneto.json`). Chose Veneto specifically because its Viveracqua-consortium structure is fragmented across many gestori (Etra, Acquevenete, AcegasApsAmga, Viacqua, SIB spa/BIM Gestione Servizi Pubblici, Alto Trevigiano Servizi), avoiding a repeat of Calabria's 1-candidate monopoly ceiling. Stage 01: 27 signals/21 sources; the model caught and correctly resolved a real entity-history fact itself — Centro Veneto Servizi's 2017 merger into Acquevenete — flagging it as no longer a separate outbound target, without needing a human correction. Stage 02: 6 candidates, all genuine utilities, 0 non-buyer leakage. Stage 03: 27 validated/17 rejected — every rejection was the same mechanical reason (`outdated`: undated Tier-B company-website sources for time-sensitive funding claims, no Tier-A corroboration), which happened to hit the "Acqua in Rete" three-operator joint project especially hard, leaving Acquevenete, AcegasApsAmga, and Viacqua with much thinner validated NRW evidence than Etra, SIB spa, and Alto Trevigiano Servizi (ATS). Gate 1 approved — a legitimate, rule-consistent outcome, not a bug. Stage 04: ATS led at 17/25 (momentumSignal 5/5 on a fresh €26M July-2026 financing facility), Etra and SIB spa tied at 15/25; the "distress ≠ momentum" rubric line (added during the Sicilia run) applied correctly again — Etra's and SIB spa's own delay flags scored momentumSignal 0, not inflated. AcegasApsAmga scored lowest (4/25) reflecting its evidence loss. Stage 05 (`--brief-all`): 5 of 6 briefed (AcegasApsAmga correctly dropped — no validated evidence survived Stage 03 to brief from), 0 dropped for outreach language, fact/interpretation splits clean throughout. 2 contacts found: Andrea Mirandola (Etra, ICT Manager) and Francesca Cavaletto (Acquevenete, BIM Coordinator), both sourced via press mention / company team page. Gate 2 approved. Adding the 11th eval case broke the 2 tests hardcoding the eval-case count (10→11) — fixed in `tests/eval.harness.test.mjs`. `contacts:build` run afterward: **22/30 contacts, from 6 approved runs** (up from 20/30 after 5). 221 tests passing.
- **2026-07-27** — Post-Veneto spot-check and a standing reporting-language correction, both from user feedback. (1) Gate-2-accountability gap: the Veneto summary had described contacts as "unverified pending your review" even though Gate 2 was self-approved under the standing auto-run instruction with no actual human read-through — contradictory framing. Correction going forward (not applied retroactively to past run descriptions): whenever a gate is cleared via the standing auto-approve instruction rather than a real human review, say so explicitly (e.g. "Gate 2 self-approved per standing instruction — no genuine ambiguity found; contacts have not had independent human verification") instead of language implying a review is still pending. (2) Spot-checked the three headline euro figures from the Veneto run, on the reasoning that it was the first region to close with zero corrections across all five stages, and that was worth independently confirming rather than assuming as pipeline maturity: ATS's €26M July-2026 financing (confirmed exactly against NordEst News), Etra's €43.73M PNRR project incl. €20.84M PNRR share (confirmed exactly against the OpenPNRR government portal), and SIB spa's €25.65M NRW programme incl. €20.45M PNRR share and 35%-by-end-2025 target (confirmed exactly against sibspa.it, cross-checked against an independent search). One near-miss during the check: OpenPNRR's page briefly appeared to say the SIB spa project covers 14 municipalities against the run's 16 — re-verified against SIB spa's own project page (which names all 16) and an independent search corroborating the same 16; the "14" was an artifact of the fetch tool's own summarization, not an error in the run's evidence. Net result: Veneto's zero-correction outcome holds up as genuine on these three figures, not a miss. Pushed to `origin/main` (`deb14e3`) after the spot-check.
- **2026-07-27** — Toscana regional run, full Stage 01→05 live, both gates self-approved per the standing auto-run instruction — no genuine ambiguity found (`runs/2026-07-27T11-39-25-488Z__cac519a3`, new case `evals/cases/12-water-utilities-italy-toscana.json`). Chose Toscana because its six-ATO structure (Publiacqua/Firenze, Acque S.p.A./Pisa, Acquedotto del Fiora/Grosseto-Siena, GAIA/Lucca-Pistoia-Massa, ASA/Livorno-Elba, Nuove Acque/Arezzo) is fragmented like Sicilia/Trentino/Veneto rather than a single monopoly like Calabria's Sorical. Stage 01: 22 signals/14 sources, all six gestori surfaced with real NRW/funding signals and organisation-scale data, confidence and uncertainty notes handled cleanly (e.g. explicitly flagging a 380k–400k population range discrepancy for Acquedotto del Fiora rather than picking one silently). Stage 02: 6 candidates, one per ATO, 0 non-buyer leakage. Stage 03: 21 validated/21 rejected (19 duplicate, 2 outdated) — every "duplicate" rejection was a candidate-level relevanceEvidence claim restating a fact already validated once at the signal level from the identical source, which is exactly the same-fact-consistency rule (added during the Trentino run) working as intended, not evidence loss; nothing candidate-blocking. Gate 1 approved. Stage 04: all 6 scored (16–20/25), no drops, no rubric red flags. Stage 05 (`--brief-all`): all 6 briefed, 0 dropped, 4 new contacts found (Luciano Caroti/Publiacqua, Michela Ticciati/Acquedotto del Fiora, Michele Del Corso/ASA, Francesca Menabuoni/Nuove Acque); Acque S.p.A. and GAIA honestly returned none. No outreach language in any brief. Gate 2 approved, following the 2026-07-27 reporting-language correction — explicitly noted as self-approved with no independent human verification of contacts yet, not "pending review." Adding the 12th eval case broke the 2 tests hardcoding the eval-case count (11→12) — fixed in `tests/eval.harness.test.mjs`. `contacts:build` run afterward: **26/30 contacts, from 7 approved runs** (up from 22/30 after 6). 221 tests passing.
- **2026-07-28** — Second-opinion review of the Toscana run (Claude.ai reviewing the chat summary, not the artifacts) surfaced a real prose-vs-data mismatch: the summary described GAIA as "GAIA/Livorno-Elba," but `02-candidates.json` always had the correct `"Toscana — provinces of Lucca, Pistoia, Massa-Carrara (ATO1)"` — the error was in the recap, not the pipeline. This is the **second** time a natural-language summary has misdescribed correctly-generated artifacts (first was Veneto's "unverified pending your review" language on a self-approved Gate 2, 2026-07-27 entry above). Schemas validate the JSON; nothing validates the prose describing it, and the prose is what gets read. Fixed the wording at the point above where it appears; no data change needed. Worth remembering as a category of error distinct from pipeline bugs.
- **2026-07-28** — GEAL S.p.A. (`cand-7`, added same day as a manual Stage-02 correction for the missed 7th ATO1 gestore) turned out to be a real entity but not a real lead: research had surfaced its January-2025 PNRR project but missed the concession fight that was GEAL's dominant story through 2025–2026. Verified independently (not taken from the user's summary): GEAL's 30-year concession (since 1995, 52% Lucca Holding S.p.A./48% Acea S.p.A.) expired 31 December 2025; AIT began a transfer ("subentro") process to GAIA (ATO1's designated gestore) in July 2024 that stalled by August 2025 over unsigned union agreements and an unfinanced ~€27.9M buyout value owed to Acea; the Comune di Lucca won at TAR in April 2025, AIT appealed to the Consiglio di Stato, and in April 2026 the Consiglio di Stato annulled AIT's rejection on procedural grounds and sent the question back to AIT's Assembly — still unresolved at the time of this research. A one-year technical extension in November 2025 moved the concession's expiry to 31 December 2026, explicitly to preserve continuity rather than settle the dispute. Added as validated evidence (`ev-50`–`ev-53`, four new sources `src-19`–`src-24`, all independently fetched, not taken on the user's word). Re-scored `momentumSignal` 3→1 (justification: the PNRR project is real and ongoing, but not a stable, actionable signal given the entity's own near-term expiring right to operate and unresolved litigation — "contested subentro in progress," not evidence pointing to either GEAL or GAIA cleanly); `totalScore` 16→14. Marked `leadQualification: excluded` in the brief (additive field, same pattern as Calabria's `openOpportunities`) with the reason recorded in plain language. Kept the Salvatore Pipus contact (real, verified — confirmed AD + Direttore Generale at the May 2025 shareholder assembly) but corrected his `targetRoleCategory` to `"other"` with an explicit note that CEO-level is not one of the four target functions. Considered but did not validate the "PNRR completion transfers to GAIA" narrative some early-2025 coverage implied — the most-recent (April 2026) sources show the opposite emphasis (the 2025 extension was granted specifically so GEAL, not GAIA, finishes its own PNRR work) — so this was left unclaimed rather than asserted either way. Checked whether GAIA's own `momentumSignal` should rise given the pending subentro: found no evidence GAIA has secured the buyout financing or otherwise treated the transfer as its own active initiative — proposed no change, on the reasoning that a contested, stalled legal process is not the same as "GAIA has this." All edited artifacts re-validated against schemas (`totalScore` sums re-verified in code); 221 tests still passing throughout.
- **2026-07-28** — Added an entity-continuity requirement to Stage 02 (same two-file pattern as the earlier organisationType and parent/subsidiary scope rules): `02_candidate-discovery/CONTEXT.md` and `lib/workflow/stage02.mjs`'s system prompt/user-message builder now require every future candidate to carry an `entityContinuity` object (`status`, `concessionExpiry`, `pendingTransfer`, `litigation`), explicitly requiring "unknown" over a guessed "stable" when the signals don't say. Prospective only — not retroactively re-run on past regions. Grepped all six previously-approved runs' candidate lists for consolidation/merger/concession-expiry language as a one-time check: Calabria's Sorical (documented ~400-municipality consolidation, already known) and Veneto's Acquevenete (2017 Centro Veneto Servizi merger, already caught by the model itself at the time) were the only hits, and neither shows an active unresolved dispute the way GEAL did — no other GEAL-shaped surprise found in the existing approved candidate lists. Logged as `ASSUMPTIONS-TO-REVISIT.md` items 9 (no scoring dimension for entity continuity — raise with Frans/Elena) and 10 (Sicilia ATO 3 Messina/ATO 7 Trapani not fully assigned per ARERA, same miss-shape as GEAL, deliberately not chased).
- **2026-07-28** — The prior entry's "still unresolved... contested subentro in progress" conclusion was itself stale by about a month — a second independent-verification pass (checking further forward in time, not re-reading the same sources) found the matter had actually settled between the two checks. Two later developments, verified by direct fetch: (1) 27 April 2026 — TAR Toscana rejected Lucca's appeal **on the merits** (missing three required technical conditions), a separate, later ruling from both the April 2025 TAR decision and the 28 April 2026 Consiglio di Stato ruling already on record — clearing the legal path to GAIA; (2) 27 June 2026 — Lucca's mayor confirmed the Comune would not appeal further, ending the litigation; negotiations with GAIA are proceeding on employee guarantees, territorial representation, and investment/tariff terms, with the mayor's own estimate that completion is unlikely before roughly 2027. Added as `ev-54`/`ev-55`, two new sources (`src-25`/`src-26`). The failure mode is specific and now codified: search over-indexes on dramatic ruling-moment coverage ("colpo di scena dal Consiglio di Stato") and under-indexes on the quiet moment that actually settles a matter (a council deciding not to appeal) — a genuinely independent verification pass can still land on a stale conclusion if it doesn't explicitly search *forward* past the most recent dramatic event found. Added an "entity-continuity recency rule" to `evidence-standards.md` and mirrored it in `stage02.mjs` (same two-file pattern) requiring exactly this forward-check, distinct from the existing source-age recency rules. GEAL's brief corrected to "being absorbed into GAIA, terms/timing under negotiation" — kept excluded from qualified leads (conclusion unchanged, now better evidenced). Separately corrected a real double-counting bug in the prior entry: GEAL's `momentumSignal` had been scored down *and* the candidate excluded for the same underlying continuity fact. Reverted `momentumSignal` to a clean, evidence-only score (3/5, `totalScore` back to 16 — the original pre-continuity-correction number), with the continuity risk carried entirely by the additive `leadQualification.excluded` field. Reasoning: a scoring dimension should answer one question, so "what would this candidate be worth if it weren't being absorbed" stays answerable — logged as the resolved interim policy in `ASSUMPTIONS-TO-REVISIT.md` item 9. Added `entityContinuity` to GAIA's own candidate record (pending inbound expansion, not yet reflected in its operating figures) and proposed — not yet applied — a `momentumSignal` 1→3 / `totalScore` 16→18 delta for GAIA, on the reasoning that a settled-on-the-merits, actively-negotiated inbound transfer is real recent news tied to network modernization even before completion, but should not be scored as if GAIA already has the network/PNRR project in hand. Gate 2 remains pending final user approval of the GEAL brief, the GAIA delta, and the score-vs-disqualifier policy. 221 tests passing throughout; no code path other than the two files above changed.
- **2026-07-29** — User rejected the proposed GAIA `momentumSignal` 1→3 delta, and the reasoning was better than mine: I'd left `utilitySize` unchanged because GAIA's absorption of Lucca "hasn't grown yet," then raised `momentumSignal` on that same not-yet-happened absorption — one underlying future state, two inconsistent treatments in the same session, right after establishing the one-dimension-one-question principle on GEAL a few hours earlier. Consistency within a session isn't automatic just because a rule was stated once. Fixed: GAIA's `totalScore` stays 16, all five dimensions unchanged. The Lucca absorption instead went into `openOpportunities` (04-scores.json, cand-4) — the same additive field used for Calabria's SFNIISSI signal — carrying `ev-54`/`ev-55` forward as real, sourced, commercially relevant context without touching the score. Verified it actually surfaces in `05-brief.md` (same check run on Calabria's `openOpportunities`) rather than being silently dropped — it does, in both GAIA's own section and cross-referenced from GEAL's. Kept the `entityContinuity` addition to GAIA's candidate record (a factual record, not a score change, so it stood). 221 tests passing; schema-validated before and after.
- **2026-07-26** — Contact-yield diagnostic after both regions closed: Calabria (1 candidate) found 0 contacts, Trentino (18 candidates, mostly small municipalities) found 1, vs. Sicilia's (7 utility-scale candidates) 5 — asked whether this was expected variance by candidate type or a pipeline issue. `toolActivity` is hardcoded `'not_available'` in every `localCli.mjs` provider result (text-mode CLI output structurally doesn't expose tool-call counts), so there was no log to check search effort against. Instead spot-checked independently: ran a few generic web searches for Sorical's leadership and found real, sourced, named individuals in target-role categories (e.g. Giuseppe Sorrentino, "Direttore Operativo Idrico") that the live Stage 05 run had missed entirely — ruling out "large utilities lack public-facing staff info" as the explanation for Calabria specifically (Sicilia's comparable-scale utilities did yield contacts), while confirming Trentino's low yield is genuinely explained by candidate type (small municipal offices). Re-ran Stage 05 for the Calabria run alone (`npm run workflow:stage05 -- --run ...705731b4 --brief-all`) to test for run-to-run search non-determinism — confirmed: the second pass found 4 real contacts, including Giovanni Paolo Marati (Direttore Generale, Feb 2026 press mentions), matching what the independent spot-check had already surfaced. Backed up the original Gate-2-approved (0-contact) brief to `gate2-approved-backup/` before overwriting, same pattern as the V3.8 curated→brief-all supersession; reset `reviewGates.gate2` to `pending` since the new content wasn't what had actually been reviewed, then re-reviewed properly. One of the 4 new contacts, Antonio Voci, carried a real identity-collision flag the model surfaced itself (two different LinkedIn profiles of the same name at Sorical, unresolved) — same risk category as the Trentino Acque name collision, not a normal verification note — so it was dropped rather than kept with a caveat; the other three (Marati, Iennarella, Locanto) were kept as-is. `05-briefs.json`/`05-brief.md` corrected, re-validated against the schema, Gate 2 re-approved. `contacts-master.md` rebuilt: **20/30 contacts, still 5 approved runs** (Sorical's 0→3 is the only count change). Conclusion for future runs: contact yield varies run-to-run due to live search non-determinism (consistent with the project's existing brief-all-vs-curated finding), and a genuinely low result should be spot-checked or re-run once before being read as a structural candidate-type limit — 217 tests passing throughout, no code changed.
